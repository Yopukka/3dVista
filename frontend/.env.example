# Frontend environment variables (Vite).
# NOTE: anything prefixed with VITE_ is bundled into public client-side JS.
# Never put secrets here — only public configuration like the API base URL.

VITE_API_URL=http://localhost:8000/api
